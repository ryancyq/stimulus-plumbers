<h1>Calendar (Turbo / SSR)</h1>

<div id="calendar">
  <% if @view == "month" %>
    <%= sp_calendar_turbo_month(date: @date) %>
  <% elsif @view == "year" %>
    <%= sp_calendar_turbo_year(date: @date) %>
  <% elsif @view == "decade" %>
    <%= sp_calendar_turbo_decade(date: @date) %>
  <% else %>
    <%= sp_calendar_turbo(selectable: @selectable, show_other_months: @show_other_months) %>
  <% end %>
</div>
