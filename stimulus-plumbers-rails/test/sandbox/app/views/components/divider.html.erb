<h1>Divider components</h1>

<%# ── Plain divider ───────────────────────────────────────────────────── %>
<section aria-label="Plain divider">
  <p>Section above</p>
  <%= sp_divider %>
  <p>Section below</p>
</section>

<%# ── Labeled divider ─────────────────────────────────────────────────── %>
<section aria-label="Labeled divider">
  <p>Sign in with email</p>
  <%= sp_divider("OR") %>
  <p>Sign in with Google</p>
</section>
